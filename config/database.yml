# MySQL
development:
  adapter: mysql
  database: bind_dns_development
  host: localhost
  username: root
  password:

# Warning: The database defined as 'test' will be erased and
# re-generated from your development database when you run 'rake'.
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  database: bind_dns_test
  host: localhost
  username: root
  password:

production:
  adapter: mysql
  database: bind_dns_production
  host: localhost
  username: root
  password: dont-use-root-here
