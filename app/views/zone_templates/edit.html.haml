%h1.underline Edit Template

- form_for( :zone_template, @zone_template, :url => { :action => 'update', :id => @zone_template } ) do |f|
  %table
    %tr
      %td Name
      %td= f.text_field :name
    %tr
      %td TTL
      %td= f.text_field :ttl
    %tr
      %td= link_to 'Cancel', templates_path
      %td= submit_tag 'Save'