%h1.underline= "Results for \"#{@search_parameter}\""
%br/

%h2.underline Zones

- unless @results.blank?
  - for zone in @results do
    = link_to zone.name, zone_path( zone )
    %br/

= will_paginate @zones