%h1.underline Latest 10 zones

%table
  = render :partial => '/zones/zone', :collection => @latest_zones

&nbsp;
%h1 Search

- form_tag( { :controller => :search, :action => :results }, { :method => :get } ) do |f|
  = text_field_tag :q
  = submit_tag "Search"

&nbsp;
%h1.underline New zone

= render :partial => '/zones/new'